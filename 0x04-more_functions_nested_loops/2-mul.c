#include <stdio.h>
#include "main.h"

/**
* mul - multiplies two digits
* @a: first digit
* @b: second digit
* Return: 0 (success)
*/

int mul(int a, int b)
{
int product;
product = a * b;
return (product);
}
